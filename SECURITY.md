# 安全与隐私

## 支持范围

本项目是本地研究工具，不提供远程账号系统。安全问题主要包括：

- 本地路径或分析数据意外泄露；
- 通过文件名、路径或导出内容注入页面；
- 恶意视频导致的资源耗尽或解码器问题；
- 依赖漏洞；
- 本地服务被非预期网络访问。

## 默认安全边界

- 服务只监听 `127.0.0.1:8765`，不对局域网或公网开放。
- 视频由本机 OpenCV 读取，不上传远程服务。
- `local-data/`、常见视频格式和环境文件被 Git 忽略。
- UI 在写入用户文本时应进行 HTML 转义。
- 导出文件可能包含本机绝对路径，公开分享前必须脱敏。

## 报告安全问题

在仓库启用 GitHub Security Advisories 后，请优先使用私密安全报告功能。

在此之前，不要创建包含以下信息的公开 Issue：

- 可利用的完整攻击步骤；
- 用户绝对路径或个人信息；
- 令牌、Cookie、SSH 密钥或其他凭证；
- 未公开的比赛录像。

请联系仓库所有者，并提供：

- 受影响版本或提交；
- 风险影响；
- 最小复现；
- 建议修复方向；
- 是否已经公开披露。

## 维护者响应目标

这是一个早期研究项目，不承诺商业级 SLA。维护者应尽量：

- 7 天内确认报告；
- 对高风险问题优先限制影响范围；
- 修复后在发布说明中给出不泄露利用细节的摘要。

## 依赖

Python 和 Node 依赖均应通过锁定文件或固定版本安装。升级 OpenCV、Flask、vinext 或构建工具时，需要重新执行本地启动和构建验证。
